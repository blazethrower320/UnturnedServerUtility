<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:converters="using:UnturnedServerUtility.Converters"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="using:UnturnedServerUtility.Models"
             xmlns:viewmodels="using:UnturnedServerUtility.ViewModels"
             mc:Ignorable="d" d:DesignWidth="650" d:DesignHeight="410"
             x:Class="UnturnedServerUtility.GUI.Styles.ServerSelection"
             x:DataType="viewmodels:ServerSelectionViewModel">
    
    <UserControl.Resources>
        <converters:ProcessToColorConverter x:Key="ProcessToColorConverter" />
    </UserControl.Resources>
    
    <Border Background="#18191A">
        <Grid RowDefinitions="Auto, *">
        <!-- Top Toolbar Actions -->
        <Border Grid.Row="0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,10,0" Spacing="10">
                <Button>Create Server</Button>
                <Button>Delete</Button>
                <Button Click="ListExecutables_Button_Click">List Executables</Button>
                <Button Click="InstallCMD_Button_Click">Install SteamCMD</Button>
                <Button Click="UninstallCMD_Button_Click">Uninstall SteamCMD</Button>
            </StackPanel>
        </Border>

		<!-- Main Server Content Area -->
		<Grid Grid.Row="1" Margin="10">
			<ItemsControl ItemsSource="{Binding ServerList}">
				<ItemsControl.ItemTemplate>
					<DataTemplate x:DataType="models:ServerCollection">
                        
						<Border Background="#202225" 
                            CornerRadius="6" 
                            Padding="12,8" 
                            Margin="0,4"
                            HorizontalAlignment="Stretch">
                            
                            <Grid VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.Styles>
                                    <Style Selector="Button">
                                        <Setter Property="Padding" Value="10,4"/>
                                        <Setter Property="Margin" Value="3,0"/>
                                        <Setter Property="FontSize" Value="12"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="CornerRadius" Value="4"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Style>
                                </Grid.Styles>
                                <TextBlock Text="{Binding severName}" 
                                           Foreground="{Binding Tracker.Process, Converter={StaticResource ProcessToColorConverter}}" 
                                           FontWeight="SemiBold"
                                           FontSize="14"
                                           VerticalAlignment="Center" 
                                           Grid.Column="0" />

                                <Border Grid.Column="1" 
                                        Background="#2F3136" 
                                        CornerRadius="4" 
                                        Padding="8,4" 
                                        Margin="0,0,12,0" 
                                        VerticalAlignment="Center">
                                    <TextBlock Foreground="#B9BBBE" FontSize="12" FontWeight="Medium">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}{0}/{1} Players">
                                                <Binding Path="playerCount" />
                                                <Binding Path="playerMax" />
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </Border>
                                <Button Click="ServerSelection_Button_Click" CommandParameter="{Binding}" Grid.Column="2" Background="Purple">Console</Button>
                                <Button Click="StartServer_Button_Click" CommandParameter="{Binding}" Grid.Column="3" Background="#43B581">Start</Button>
                                <Button Click="RestartServer_Button_Click" CommandParameter="{Binding}" Grid.Column="5" Background="#FAA61A">Restart</Button>
                                <Button Click="StopServer_Button_Click" CommandParameter="{Binding}" Grid.Column="4" Background="#F04747">Stop</Button>
                            </Grid>
                        </Border>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
		</Grid>
    </Grid>
    </Border>
</UserControl>